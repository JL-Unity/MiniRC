---
description: 需求与框架实现冲突时先讨论改进点，不硬塞实现
alwaysApply: true
---

# 框架冲突时先对齐，不硬写需求

在实现需求的过程中，若发现与**项目内已有框架、架构约定或既有实现**明显冲突（例如：会破坏分层、重复造轮子、绕过已有生命周期、与现有 API 语义不一致、可维护性差等）：

1. **不要**为「交差」而硬写补丁式代码、大量特例分支或复制粘贴式实现。
2. **主动说明**冲突点：当前需求想怎么做、框架/现有代码现在怎么做、二者为何不一致。
3. **和用户讨论**框架或基础层**可以改进的方向**：建议改哪里、预期收益、改动面与风险；说明**原因**（可维护性、一致性、性能、扩展性等）。
4. 把选择权交给用户：例如坚持在业务层 workaround、还是抽时间改框架/抽象、或调整需求边界。

若用户已明确选择某一侧（例如「先 hack，以后再重构」），再按该约束实现。
