---
description: 在复杂或非显而易见的代码处补充注释，便于维护与审阅
alwaysApply: true
---

# 复杂代码注释

在编写或修改代码时，对**复杂、非显而易见**的部分**主动添加简短注释**（中文或项目已有语言，与文件内风格一致）。

## 建议加注释的情形

- 算法、数学或物理公式（说明意图、单位、近似条件）。
- 多分支状态机、标志位组合、与业务强绑定的约定。
- 与框架/引擎行为相关的坑、顺序依赖、时序（例如 FixedUpdate vs Update）。
- 临时方案、兼容旧数据、性能权衡等非一眼能看出的设计决策。

## 避免

- 对单行简单赋值、标准 API 调用重复叙述「代码在做什么」。
- 冗长段落式注释；优先**块注释说明「为什么」**，必要时用 `//` 标关键点。

## 粒度

注释应帮助**下次修改的人**快速建立心智模型；若某段逻辑需要读者「猜半天」，就应加注释或拆函数并命名清晰。
