public enum GameEndReason
{
    Success,
    Failed
}

public enum PlayerDeadType
{
    None,
    KillZone
}

public enum Difficulty
{
    Easy,
    Normal,
    Hard,
    Extreme,
}

public enum ColorType
{
    Green,
    Blue,
    Orange,
    Red,
}